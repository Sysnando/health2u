<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Local development: allow cleartext to the Android emulator's
         host loopback (10.0.2.2) and localhost when the admin-service
         backend runs on the host machine. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>

    <!-- Production / staging APIs: HTTPS only. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">dev-api.health2u.com</domain>
        <domain includeSubdomains="true">staging-api.health2u.com</domain>
        <domain includeSubdomains="true">api.health2u.com</domain>
    </domain-config>
</network-security-config>
